[project]
name = "agent-runtime"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rajatrc1705", email = "rajatrc1705@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
agent-runtime = "agent_runtime:main"

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
